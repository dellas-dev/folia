import { PagePlaceholder } from '@/components/app/layout/page-placeholder'

export default function AffiliatePage() {
  return (
    <PagePlaceholder
      eyebrow="Affiliate"
      title="Referral tooling is planned for Phase 6."
      description="The route already exists inside the app shell so affiliate creation, stats, and locked-tier behavior can be added without changing navigation."
      phaseLabel="Phase 6"
    />
  )
}
